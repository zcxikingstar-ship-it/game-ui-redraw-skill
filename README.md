# game-ui-redraw

Codex Skill：识别游戏界面截图，在用户确认元素清单后，调用内置生图重新生成无字按钮、图标、面板和背景素材。

它不会把原截图抠图作为交付素材。

## 安装

下载并解压 `game-ui-redraw-skill.zip`，将 `game-ui-redraw` 文件夹放入：

```text
~/.codex/skills/
```

重启 Codex 后，上传游戏截图并使用：

```text
$game-ui-redraw
```
